/** Shared motion tokens for the letterpress/HUD overlays. */

/** The house easing curve — a soft, slightly overshooting ease used by every
 *  overlay entrance so panels, HUDs and the nav drawer share one feel. */
export const EASE = [0.22, 0.61, 0.27, 1] as const;
