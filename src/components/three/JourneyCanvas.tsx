'use client';

/**
 * JourneyCanvas — Phase 4 stub.
 * Phase 4 will replace the internals with a @react-three/fiber Canvas that
 * renders the 3-D journey rig. The contract (fixed, -z-10, aria-hidden) is
 * stable — do not alter it.
 */
export function JourneyCanvas() {
  return (
    <div
      aria-hidden="true"
      className="fixed inset-0 -z-10 bg-gradient-to-b from-[#252530] to-[#070810]"
    />
  );
}
