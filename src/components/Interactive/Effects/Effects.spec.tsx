import React from 'react';
import { render } from '@testing-library/react';

import Effects from './Effects';

describe('components/Effects', () => {
  it('renders', () => {
    render(<Effects />);
  });
});
